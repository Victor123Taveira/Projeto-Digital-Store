import React from 'react';
import logo from '../../assets/logo.png'; // ajuste o caminho se necessário

const Logo = () => (
  <img src={logo} alt="Drip Store" width={253} height={44} />
);

export default Logo;
